import { Reactor } from './Reactor';

export class Response extends Reactor<Response.Options> implements Reactor.Relay<Response.Options> {

    public relay(options: Response.Options) {
        
    }
}

export namespace Response {

    export interface Options {

    }
}